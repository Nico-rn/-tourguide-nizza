services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      # SQLite-Datei liegt auf dem Host -> übersteht Container-Neustarts
      - ./data:/app/data
    restart: unless-stopped
